# Specification Index

Read these documents before converting or restructuring recipes:

1. [`spec.md`](spec.md) — recipe Markdown conversion rules, category policy, frontmatter requirements, source attribution, and local testing expectations.
2. [`quantity-scaling.md`](quantity-scaling.md) — reserved `{...}` quantity token syntax, runtime scaling behaviour, and automated test requirements (draft, not yet implemented).

For future larger specs, add them here in reading order and keep `spec.md` as the active recipe-content standard until it is deliberately renamed.
